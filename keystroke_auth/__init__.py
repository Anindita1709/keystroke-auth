"""Keystroke dynamics authentication with One-Class SVM and Binary SVM."""
import logging

__version__ = "0.1.0"
__author__  = "Your Name"

logging.getLogger(__name__).addHandler(logging.NullHandler())
